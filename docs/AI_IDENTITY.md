# AI Identity

The AI instance is not the language model.

The AI instance owns:

- identity
- personality
- memories
- journal
- preferences
- long-term history

The language model only provides reasoning and language capabilities when required.

Changing models must never create a different AI.

Identity persists independently from capabilities.

Every AI instance is unique.

Users may create multiple independent AI instances.

Each instance owns its own evolution.
